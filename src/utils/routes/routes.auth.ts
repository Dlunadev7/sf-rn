export enum AuthRoutes {
  LOGIN = 'index',
  FORGOT_PASSWORD = 'forgot-password.screen',
  NEW_PASSWORD = 'new-password.screen',
  NEW_PASSWORD_SUCCESS = 'new-password-success.screen',
}

export enum AuthRoutesLink {
  LOGIN = '/(auth)/',
  FORGOT_PASSWORD = '/(auth)/forgot-password.screen',
  NEW_PASSWORD = '/(auth)/new-password.screen',
  NEW_PASSWORD_SUCCESS = '/(auth)/new-password-success.screen',
}
