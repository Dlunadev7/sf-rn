export enum AuthRoutesService {
  LOGIN = '/auth/login',
  RECOVERY_PASSWORD = '/mail/recoveryPassword',
  NEW_PASSWORD = '/user/changePassword',
}

export enum AuthRoutesActions {
  LOGIN = 'auth/login',
  RECOVERY_PASSWORD = 'auth/recoveryPassword',
  NEW_PASSWORD = 'user/changePassword',
}
